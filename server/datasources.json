{
  "mongodb": {
    "name": "mongodb",
    "connector": "mongodb",
    "url": "",
    "host": "${MONGODB}",
    "username": "${MONGODB_USER}",
    "password": "${MONGODB_PASSWORD}",
    "port": 27017,
    "database": "todo"
  }
}
